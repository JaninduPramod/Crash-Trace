# Dependency Injection (DI)

This folder contains classes and modules for dependency injection setup in the app.

- Use this folder for Hilt or Dagger modules and components.
- Configure app-wide dependencies here.

See the `repository` and `viewmodel` folders for dependent classes.
