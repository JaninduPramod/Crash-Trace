# ViewModel

This folder contains ViewModel classes for the app.

- ViewModels hold and manage UI-related data in a lifecycle-conscious way.
- Use this folder for classes extending ViewModel.

See the `repository` folder for data sources and the `screens` folder for UI usage.
