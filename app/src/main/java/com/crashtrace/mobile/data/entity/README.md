# Entity

This folder contains data classes that represent the database tables (entities) for the app.

- Each entity should be annotated with @Entity.
- Entities define the schema for Room database tables.

See the `dao` and `db` folders for related components.
