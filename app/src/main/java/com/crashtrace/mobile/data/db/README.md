# Database (DB)

This folder contains the Room database class and related database setup files for the app.

- The main database class should extend RoomDatabase.
- Configure DAOs and entities here.

See the `dao` and `entity` folders for more details.
