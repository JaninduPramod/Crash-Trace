# Components

This folder contains reusable UI components for the app.

- Use this folder for custom composables, widgets, and UI elements.
- Components should be stateless and reusable across screens.

See the `screens` folder for screen-level composables.
