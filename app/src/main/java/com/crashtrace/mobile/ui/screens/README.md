# Screens

This folder contains composable functions representing the app's screens.

- Each screen should be a separate composable function.
- Use this folder for top-level UI screens.

See the `navigation` folder for navigation setup.
